# ⚠️  IMPORTANT: Do NOT change the Confluent Cloud cluster details below.
# These values are specific to the TEST environment and must remain constant.
# Contact the platform team if you need to update any of these values.

service_name: "payment-service"
organization_id: "db7bf8c7-4d1d-4e84-a594-8f771fe8e13b"
environment_id: "env-ozk1yj" # TEST environment - do not change
kafka_cluster_id: "lkc-oj0vro" # TEST cluster - do not change
rest_endpoint: "https://pkc-p11xm.us-east-1.aws.confluent.cloud:443" # TEST REST endpoint - do not change
schema_registry_id: "lsrc-g7g3om" # TEST schema registry - do not change
topics:
  - name: "payment-processed"
    partitions: 6
    replication_factor: 3
    config:
      retention.ms: "604800000"
# SCHEMAS (OPTIONAL)
# If your service needs a schema:
# 1. Create an Avro schema file under schemas/test/
# 2. Uncomment and customize the block below
# If you don't need a schema, leave this section commented.
#
# schemas:
#   - subject: "my-service-events-value"
#     schema_file: "schemas/test/my-service-schema.avsc"

schemas: []

# ACCESS CONFIGURATION (OPTIONAL)
# Define service accounts with their permissions in one shot.
# Each entry creates: 1 service account + 1+ ACLs (one per topic).
# Terraform will auto-generate principals and crn_patterns.
# API keys stored in GitHub secrets as CONFLUENT_<NAME>_API_KEY and CONFLUENT_<NAME>_API_SECRET
#
# access_config:
#   - name: "payment-producer"
#     description: "Producer for payment events"
#     role: "DeveloperWrite"
#     topics:
#       - "payment-processed"
#   - name: "payment-consumer"
#     description: "Consumer for payment events"
#     role: "DeveloperRead"
#     topics:
#       - "payment-processed"

access_config: []