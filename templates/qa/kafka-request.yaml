# ⚠️  IMPORTANT: Do NOT change the Confluent Cloud cluster details below.
# These values are specific to the QA environment and must remain constant.
# Contact the platform team if you need to update any of these values.

service_name: "payment-service"
environment_id: "env-ozk1yj"
kafka_cluster_id: "lkc-oj0vro"
rest_endpoint: "https://pkc-p11xm.us-east-1.aws.confluent.cloud:443"
schema_registry_id: "lsrc-g7g3om"
topics:
  - name: "payment-processed"
    partitions: 6
    replication_factor: 3
    config:
      retention.ms: "604800000"
# SCHEMAS (OPTIONAL)
# If your service needs a schema:
# 1. Create an Avro schema file under schemas/qa/
# 2. Uncomment and customize the block below
# If you don't need a schema, leave this section commented.
#
# schemas:
#   - subject: "my-service-events-value"
#     schema_file: "schemas/qa/my-service-schema.avsc"
acls:
  - principal: "User:sa-payment-producer"
    role: "DeveloperWrite"
    # ⚠️  IMPORTANT: Do NOT change the crn_pattern below.
    # It contains environment-specific values (organization, environment, cluster IDs) that are QA-specific.
    # Contact the platform team if you need to update the crn_pattern.
    crn_pattern: "crn://confluent.cloud/organization=db7bf8c7-4d1d-4e84-a594-8f771fe8e13b/environment=env-ozk1yj/cluster=lkc-oj0vro/topic=payment-processed"